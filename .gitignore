node_modules
.next
out
.DS_Store
*.log
.env*.local
.env


# TypeScript incremental build cache — machine-local, changes on every build
tsconfig.tsbuildinfo

# Vercel CLI link metadata — machine-local, never committed.
.vercel
.env*
