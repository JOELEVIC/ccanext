export default function Home() {
  return (
    <main style={{ padding: 32, fontFamily: "system-ui" }}>
      <h1>ccanext</h1>
      <p>GraphQL API at <a href="/api/graphql">/api/graphql</a></p>
      <p>Query, mutation, and subscription supported.</p>
    </main>
  );
}
